Write a travel recommendation for the user.

CONVERSATION SO FAR:
{{ history }}

USER'S LATEST MESSAGE:
"{{ message }}"

ROUTE PLAN (you MUST copy this into the "route" field of your JSON output):
{{ route_json }}

SOCIAL INFO (mention meetup opportunities in your "response" text):
{{ social_info }}

TOTAL ROUTE DURATION: {{ total_duration }}
ESTIMATED TRAVEL TIME BETWEEN STOPS: {{ travel_time }}

Remember: output ONLY valid JSON with "response" and "route". Copy the route plan exactly into "route".
