/** Haversine distance in meters between two lat/lng points. */
export declare function haversineDistance(lat1: number, lng1: number, lat2: number, lng2: number): number;
/** Default GPS proximity threshold for challenge verification (meters). */
export declare const GPS_THRESHOLD_METERS = 500;
